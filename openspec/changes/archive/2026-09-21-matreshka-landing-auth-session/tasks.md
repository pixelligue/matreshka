## 1. Site session and Desktop URL

- [x] 1.1 Persist login/register tokens in `localStorage`, restore signed-in on reload, sign out clears them, and auto-handoff when `next=desktop` if a session exists; verify those paths
- [x] 1.2 Point Desktop website sign-in at `/register?next=desktop` and verify the opened URL
